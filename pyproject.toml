[project]
name = "obs-card-overlay"
version = "0.1.0"
description = "Magic the Gathering card overlay for OBS"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "eventlet>=0.39.1",
    "flask>=3.0.3",
    "flask-socketio>=5.5.1",
    "requests>=2.32.4",
]
